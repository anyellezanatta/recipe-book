import { Text } from "@/components/Text";
import { Screen } from "react-native-screens";

export const FavoritesScreen = () => {
  return (
    <Screen>
      <Text text="Favorites" size="lg" />
    </Screen>
  );
};
